package com.financetracker.model.enums;

/**
 * Enum representing budget alert types
 */
public enum AlertType {
    THRESHOLD,   // When budget reaches threshold (e.g., 80%)
    EXCEEDED,    // When budget is exceeded
    WARNING      // General warning
}
